# Fact-Checking Telegram Bot v2.0

Умный Telegram-бот для мониторинга каналов, фильтрации спама и автоматической проверки фактов с использованием двухэтапной системы анализа на базе OpenAI GPT-5 и веб-поиска.

## 🎯 Основные возможности

- **Двухэтапная система фактчекинга**:
  - Этап 1: Анализ контента → умный выбор источников для проверки
  - Этап 2: Фактчекинг по выбранным источникам с веб-поиском
- **Мониторинг Telegram каналов** в реальном времени
- **Умная фильтрация** спама, рекламы и недостоверной информации  
- **Категоризация сообщений** (новости, развлечения, другое)
- **Ручная проверка** через команду `/check`
- **Подробные логи** и отладочная информация
- **Docker поддержка** для легкого развертывания

## 🏗 Архитектура

```
├── main.py                  # Основное приложение
├── src/                     # Исходный код
│   ├── config.py           # Конфигурация
│   ├── two_stage_filter.py # Двухэтапная система фактчекинга
│   ├── debug_processor.py  # Обработка сообщений с отладкой
│   ├── command_handler.py  # Обработка команд (/check, /help)
│   ├── sources_config.py   # Управление источниками
│   └── telegram_client.py  # Telegram клиент
├── tests/                   # Тесты
├── logs/                    # Логи (volume)
├── data/                    # Данные (volume)
└── docker-compose.yml      # Docker конфигурация
```

## 🚀 Быстрый запуск

### Через Docker (рекомендуется)

1. **Клонируйте репозиторий**:
   ```bash
   git clone <repository-url>
   cd fact-checking-tg
   ```

2. **Настройте переменные окружения**:
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл с вашими токенами
   ```

3. **Запустите через Docker Compose**:
   ```bash
   docker-compose up -d
   ```

### Локальный запуск

1. **Установите зависимости**:
   ```bash
   python -m venv venv
   source venv/bin/activate  # или venv\\Scripts\\activate на Windows
   pip install -r requirements.txt
   ```

2. **Настройте .env**:
   ```bash
   cp .env.example .env
   # Отредактируйте .env с вашими API ключами
   ```

3. **Запустите бота**:
   ```bash
   python main.py
   ```

## ⚙️ Конфигурация

### Обязательные переменные окружения (.env)

```bash
# Telegram
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash  
TELEGRAM_BOT_TOKEN=your_bot_token
TARGET_CHAT_ID=your_target_chat_id

# OpenAI
OPENAI_API_KEY=your_openai_key

# Каналы для мониторинга (через запятую)
CHANNELS=@channel1,@channel2,@channel3
```

### Опциональные настройки

```bash
# Модель OpenAI (по умолчанию: gpt-5-turbo-2024-11-20)
GPT_MODEL=gpt-5-turbo-2024-11-20

# Режим фактчекинга
FACT_CHECK_MODE=two_stage

# Отладочные настройки
DEBUG_MODE=true                    # Включить отладочную информацию
SHOW_ALL_MESSAGES=true            # Показывать все сообщения (включая скрытые)
SEND_DEBUG_INFO=true              # Отправлять debug информацию в чат

# Источники
MAX_SOURCE_DOMAINS=20             # Максимум доменов для проверки
STAGE2_INITIAL_DOMAIN_LIMIT=8     # Сколько доменов использовать в первой попытке этапа 2
STAGE2_RETRY_DOMAIN_LIMIT=5       # Сколько доменов оставить при повторной попытке
FACT_CHECK_TIMEOUT=45             # Таймаут (в секундах) на одну попытку фактчекинга
FACT_CHECK_MODEL=gpt-4o                # Модель Responses API для этапа 2 (web search)
WEB_SEARCH_EFFORT=medium          # Усилие reasoning при веб-поиске (low/medium/high)
```

## 📊 Режимы работы

### 1. Обычный режим (DEBUG_MODE=false)
- Показывает только прошедшие фильтр сообщения
- Краткие результаты анализа
- Минимум логирования

### 2. Отладочный режим (DEBUG_MODE=true)
- Подробные логи всех этапов обработки
- Показ времени выполнения каждого этапа  
- Информация об использованных источниках
- Логика принятия решений

### 3. Показать все (SHOW_ALL_MESSAGES=true)
- Отображает все сообщения, включая скрытые
- Полная отладочная информация
- Статистика обработки

## 🤖 Команды бота

### `/check <сообщение>`
Ручная проверка сообщения с полным анализом:
```
/check Discord объявил новую функцию ИИ-модерации
```

### `/help`
Справка по использованию бота

## 🔍 Как работает двухэтапная система

### Этап 1: Анализ контента и выбор источников
- GPT анализирует текст сообщения
- Определяет тип контента (компания, финансы, наука, спам)
- Выбирает подходящие источники для проверки:
  - Для новостей о компаниях → официальные сайты компаний
  - Для финансовых новостей → финансовые источники  
  - Для научных новостей → научные журналы
  - Для спама → быстрая проверка без источников

### Этап 2: Фактчекинг по выбранным источникам
- Веб-поиск по выбранным доменам через OpenAI
- Анализ достоверности информации
- Категоризация: новости/развлечения/другое/скрыто
- Генерация комментария при необходимости

## 📝 Логирование

Система создает подробные логи в `logs/fact_checker.log`:

```
2024-10-01 18:21:00 - Обрабатываем сообщение из @channel
2024-10-01 18:21:00 - Полный текст: Discord объявил новую функцию...
2024-10-01 18:21:00 - Длина: 85 символов
2024-10-01 18:21:01 - ЭТАП 1: Анализируем текст для выбора источников...
2024-10-01 18:21:03 - ЭТАП 1 завершен: выбрано 15 источников
2024-10-01 18:21:03 - ЭТАП 2: Проверяем факты по 15 источникам...
2024-10-01 18:21:45 - Анализ завершен: Категория=новости, Время=42.1с
```

## 🧪 Тестирование

### Запуск всех тестов:
```bash
python run_tests.py
```

### Запуск отдельных тестов:
```bash
# Тест команд
python tests/test_check_command.py

# Тест двухэтапной системы  
python tests/test_two_stage.py

# Тест конфигурации
python tests/test_config.py
```

## 🐳 Docker

### Сборка образа:
```bash
docker build -t fact-checker .
```

### Запуск контейнера:
```bash
docker run -d \
  --name fact-checker \
  --env-file .env \
  -v $(pwd)/logs:/app/logs \
  -v $(pwd)/data:/app/data \
  fact-checker
```

### Docker Compose (рекомендуется):
```bash
# Запуск
docker-compose up -d

# Логи
docker-compose logs -f

# Остановка
docker-compose down
```

## 📁 Volumes и данные

- **`logs/`** - логи приложения (ротация: 50MB × 3 файла)
- **`data/`** - Telegram сессии и временные данные
- **`.env`** - конфигурация (монтируется read-only)

## 🔧 Настройка источников

Источники для фактчекинга настраиваются в `src/sources_config.py`:

```python
sources = {
    "general_news": ["bbc.com", "reuters.com", "..."],
    "technology": ["techcrunch.com", "venturebeat.com", "..."],
    "finance": ["bloomberg.com", "marketwatch.com", "..."],
    "company_specifics": {
        "patterns": {
            "discord": ["discord.com", "blog.discord.com"],
            "google": ["blog.google.com", "developers.google.com"]
        }
    }
}
```

## 📈 Мониторинг

### Проверка статуса:
```bash
# Docker
docker-compose ps

# Локально
ps aux | grep python
```

### Логи в реальном времени:
```bash
# Docker
docker-compose logs -f

# Локально  
tail -f logs/fact_checker.log
```

## 🚨 Решение проблем

### Бот не отвечает на команды
- Проверьте `TARGET_CHAT_ID` в .env
- Убедитесь что бот добавлен в целевой чат
- Проверьте логи на ошибки авторизации

### Ошибки OpenAI API
- Проверьте `OPENAI_API_KEY`
- Убедитесь что у аккаунта есть доступ к GPT-5
- Проверьте лимиты API

### Проблемы с Telegram API  
- Проверьте `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
- Удалите `.session` файлы и перезапустите
- Проверьте права доступа к каналам

## 🤝 Разработка

### Структура кода:
- `main.py` - точка входа, настройка приложения
- `src/config.py` - конфигурация и валидация
- `src/two_stage_filter.py` - основная логика фактчекинга
- `src/debug_processor.py` - обработка сообщений
- `src/command_handler.py` - команды бота

### Добавление новых источников:
1. Отредактируйте `src/sources_config.py`
2. Добавьте новые категории или домены
3. Перезапустите бота

### Добавление новых команд:
1. Добавьте метод в `CommandHandler`
2. Зарегистрируйте обработчик в `main.py`
3. Обновите справку в `/help`

## 📄 Лицензия

MIT License - подробности в файле LICENSE

## 👥 Поддержка

- Создайте Issue в репозитории для багов
- Pull Requests приветствуются
- Документация: смотрите код и комментарии
